const input = require("fs").readFileSync("/dev/stdin").toString().split("\n");

const [n, ...arr] = input;

function solution(n, arr) {
  for (let i = 0; i < n; i++) {
    let count = 1;
    let M = Number();
  }
}

solution(n, arr);
